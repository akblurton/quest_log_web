import styled from "styled-components";

const Panel = styled.div`
  border-radius: 8px;
  background: var(--color-background);
  border: var(--color-background-alt) 2px solid;
  padding: 16px;
  box-shadow: 0 0 3px rgba(0, 0, 0, 0.3);
`;

export default Panel;
